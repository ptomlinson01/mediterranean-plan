---
phase: 01-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - GEMINI.md
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "python execution/sync_agent_files.py --add-learning 'test' runs without error"
    - "pip install -r requirements.txt installs python-dotenv"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Remember section for learnings"
      contains: "## Remember"
    - path: "requirements.txt"
      provides: "Python dependencies"
      contains: "python-dotenv"
  key_links:
    - from: "execution/sync_agent_files.py"
      to: "CLAUDE.md"
      via: "REMEMBER_SECTION constant"
      pattern: "## Remember"
---

<objective>
Add the missing `## Remember` section to CLAUDE.md and create requirements.txt with python-dotenv dependency.

Purpose: Fix the two "creation" issues that block first-run success: --add-learning feature expects a Remember section that doesn't exist, and pip install needs a requirements.txt.

Output: Working --add-learning command and pip-installable dependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-critical-fixes/01-RESEARCH.md
@CLAUDE.md
@execution/sync_agent_files.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Remember section to CLAUDE.md and sync</name>
  <files>CLAUDE.md, AGENTS.md, GEMINI.md</files>
  <action>
Add a `## Remember` section at the end of CLAUDE.md (before any closing content if present, or at the very end). The section should follow this format:

```markdown
---

## Remember

<!-- Learnings added via: python execution/sync_agent_files.py --add-learning "text" -->

```

The sync_agent_files.py script expects the exact heading `## Remember` (defined as REMEMBER_SECTION constant on line 63). After adding to CLAUDE.md, run the sync command to propagate to AGENTS.md and GEMINI.md:

```bash
python execution/sync_agent_files.py --sync --source CLAUDE.md
```

Note: The script already has DEFAULT_SOURCE = "CLAUDE.md" (line 57), so --source is technically optional but explicit is clearer.
  </action>
  <verify>
Run: `python execution/sync_agent_files.py --add-learning "Test learning for verification" --backup`

Expected: Success message "Learning added to: CLAUDE.md, AGENTS.md, GEMINI.md"

Then verify the learning appears in all three files:
`grep -l "Test learning" CLAUDE.md AGENTS.md GEMINI.md`

Clean up test learning after verification (optional - can leave as proof of fix).
  </verify>
  <done>
`--add-learning` command works without error. All three agent files contain the `## Remember` section and any added learnings are synchronized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt</name>
  <files>requirements.txt</files>
  <action>
Create `requirements.txt` in the project root with the following content:

```
# Python dependencies for Agentic Workflows Template
# Install: pip install -r requirements.txt

python-dotenv>=1.0.0
```

Use `>=1.0.0` constraint (not `==`) to allow minor/patch updates. python-dotenv 1.x is stable with no breaking changes expected.

Do NOT use upper bounds like `<2.0` - premature constraint without known incompatibility.
  </action>
  <verify>
Run: `pip install -r requirements.txt --dry-run`

Expected: Shows python-dotenv would be installed (or already satisfied).

Alternative verification:
```bash
python -c "import dotenv; print(f'python-dotenv {dotenv.__version__}')"
```
  </verify>
  <done>
`requirements.txt` exists in project root. `pip install -r requirements.txt` successfully installs python-dotenv.
  </done>
</task>

</tasks>

<verification>
Phase 1 success criteria addressed by this plan:
1. `python execution/sync_agent_files.py --add-learning "test"` works without error [Task 1]
3. `pip install -r requirements.txt` installs python-dotenv [Task 2]

Run full verification:
```bash
# Test add-learning
python execution/sync_agent_files.py --add-learning "Verification test" --backup

# Test requirements
pip install -r requirements.txt
python -c "import dotenv; print('dotenv installed')"
```
</verification>

<success_criteria>
1. CLAUDE.md contains `## Remember` section
2. AGENTS.md and GEMINI.md contain `## Remember` section (synced)
3. `--add-learning` command succeeds without errors
4. requirements.txt exists with python-dotenv>=1.0.0
5. pip install -r requirements.txt works
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-fixes/01-01-SUMMARY.md`
</output>
